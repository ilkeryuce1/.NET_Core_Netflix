@model IEnumerable<EntityLayer.Concrete.AccountKind>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccounName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AccounName)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.AccounKindtId">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.AccounKindtId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
